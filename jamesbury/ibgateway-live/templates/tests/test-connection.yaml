apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibgateway-live.fullname" . }}-test-connection"
  labels:
    {{- include "ibgateway-live.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ibgateway-live.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
